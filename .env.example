# Public origin used for canonical URLs, hreflang and the QR codes.
SITE_URL=https://guyratzon.vercel.app

# "true" only after Guy approves launch (M11 gate). Until then every response is noindex.
INDEXING=false

# Vercel API token (team-scoped, with an expiry) — reads last production deploy dates. Server-only.
VERCEL_TOKEN=
VERCEL_TEAM_ID=team_p65xAIo5EMK0EB1JfSX74KEF

# PostHog — the portfolio's own project (not SpeCV's).
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com
